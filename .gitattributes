# .gitattributes

# Set default behavior to automatically handle text files.
# This will ensure that files are checked out with the OS-native line endings
# (CRLF on Windows, LF on Linux/macOS) unless specified otherwise.
* text=auto

# Explicitly declare text files to always use LF line endings in the repository.
# This is the standard for cross-platform projects.
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.json text eol=lf
.gitignore text eol=lf
.env.example text eol=lf

# Declare specific file types as binary to prevent Git from changing them.
*.png binary
*.jpg binary
*.onnx binary
*.safetensors binary